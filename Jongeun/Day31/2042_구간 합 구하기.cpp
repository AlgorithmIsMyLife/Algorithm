#include <bits/stdc++.h>
#define MaxN 21
int IDT[MaxN <<= 1];

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);

    int N, M, K;
    cin >> N >> M >> K;

    int idx;
    for (idx = 1; idx < N; idx <<= 2)
        ;
    
    fir(int i=0; i<N; i++)
    {
        IDT[idx]
    }


    return 0;
}
